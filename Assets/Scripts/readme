//并没有写逐行注释（其实好像根本没有注释。。。）
//bug：

//不能用raycast（周四的时候debug draw很短因为第二个变量是单位向量，后来我在那里乘了100就变长了，，然而event还是没有检测到点击的是家具）然后用了onmouse啥的，这个 就是不能单选（父物体建一个指定强行单选）不能点击空白处取消选择（是不是在背景图上加一个onmouse啥的也可以？当时忘了试一下）   
//拉出来第一个家具会自动到画面中间
//其实不能缩放窗口，屏幕坐标好像很奇怪比如右边那个栏应该x坐标在2000多但是我试了一下要写x>100来判断鼠标在右边栏，总之就是有些判断的数字都是自己写的固定的要是放到全屏就会出bug（其实全屏状态下一开始拖动就会有位置差）
//删除物体，如果删了物体之后没有点一下别处就去拖新的家具，就会有位置差（但是在右边拖图片的时候没有？这两个好像是用的一个算式。。）
//对了用来拖动的图片是建了一个方块的gameobject然后贴了张图（手动哭笑不得），没有想到更科学的方法
